{% extends 'base.html.twig' %}

{% block body %}

    {% include 'layout/menu.html.twig' %}

    {% include 'layout/navbar.html.twig' %}

    <div class="container screen_challenge mt-5">
        <h3 class="text-viaautomobile mb-4"><b>LES CHALLENGES EN COURS</b></h3>
        {% if challenge_en_cours is null %}
            <div class="alert alert-info mt-3" role="alert">
                <i class="fa fa-info"></i>&nbsp;&nbsp;Aucun challenge en cours pour le moment
            </div>
        {% else %}
            <div class="row mt-3">
                <div class="col">
                    <img class="image_challenge" src="{{ vich_uploader_asset(challenge_en_cours, 'imageFile') }}" width="225" height="150">
                </div>
                <div class="col">
                    <h5 class="text-viaautomobile"><b>{{ challenge_en_cours.titre }}</b></h5>
                    <span class="periode mt-3">Période : {{ challenge_en_cours.periode }}</span>
                    <span class="description_challenge mt-3">{{ challenge_en_cours.description }}</span>
                    <span class="description_challenge">Classement en cours :</span>
                    <div class="row podium">
                        <div class="col">
                            <span>🥇 {{ first_mandat.prenom }} {{ first_mandat.nom}} ({{ first_mandat.nombre}} mandats)</span>
                            <span>🥈 {{ second_mandat.prenom }} {{ second_mandat.nom}} ({{ second_mandat.nombre}} mandats)</span>
                            <span>🥉 {{ third_mandat.prenom }} {{ third_mandat.nom }} ({{ third_mandat.nombre }} mandats)</span>
                        </div>
                        <div class="col">
                            <span>🥇 {{ first_vente.prenom }} {{ first_vente.nom }} ({{ first_vente.vente }} ventes)</span>
                            <span>🥈 {{ second_vente.prenom }} {{ second_vente.nom }} ({{ second_vente.vente }} ventes)</span>
                            <span>🥉 {{ third_vente.prenom }} {{ third_vente.nom }} ({{ third_vente.vente }} ventes)</span>
                        </div>
                    </div>
                </div>
            </div>
        {% endif %}
        <h3 class="text-viaautomobile mt-5 mb-4"><b>LES CHALLENGES PASSÉS</b></h3>
        {% for challenge in challenges_passes %}
            <div class="row mt-3">
                <div class="col">
                    <img class="image_challenge" src="{{ vich_uploader_asset(challenge, 'imageFile')}}" width="225" height="150">
                </div>
                <div class="col mt-3 mb-4">
                    <h5 class="text-viaautomobile"><b>{{ challenge.titre }}</b></h5>
                    <span class="periode mt-3">Période : {{ challenge.periode }}</span>
                    <span class="description_challenge mt-3">{{ challenge.description }}</span>
                    <span class="description_challenge"><b>Vainqueur :</b> {{ challenge.vainqueur }}</span>
                </div>
            </div>
        {% endfor %}
    </div>

{% endblock %}